# Dev: leave empty so Vite proxies API calls to http://127.0.0.1:8000 (run backend: npm run api, or both: npm run dev:all).
# Prod: set to your deployed API origin, e.g. https://your-app.up.railway.app
VITE_API_URL=
